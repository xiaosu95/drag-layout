/**
 * @description 精灵容器类型
 * @export
 * @enum {number}
 */
export enum SpiritType {
  DEFAULT,
  ABSOLUTE,
  FIXED,
  BLOCK_CONTAINER,
  INLINE_CONTAINER,
}

/**
 * @description 屏幕编辑模式
 * @export
 * @enum {number}
 */
export enum EditMode {
  DEFAULT = 'default',
  FIXED = 'fixed'
}
